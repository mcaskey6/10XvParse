# SRA download + dump (prefetch, fasterq-dump) and gzip compression of the reads.
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  - sra-tools=3.0.3
  - pigz     # unpinned: conda-forge dropped 2.4; any recent pigz is fine here
