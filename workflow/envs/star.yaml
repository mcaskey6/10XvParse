# STAR: genome index build and STARsolo alignment for gene-body coverage. gawk
# computes the genome-size-dependent --genomeSAindexNbases in the star_index rule.
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  - star=2.7.11b
  - gawk
