# seqtk: read subsampling (`seqtk sample -2`) and R2 trimming (`seqtk trimfq`),
# with pigz to recompress the output.
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  - seqtk=1.2
  - pigz     # unpinned: conda-forge dropped 2.4; any recent pigz is fine here
